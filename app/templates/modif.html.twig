{% extends "base_auth.html.twig" %}

{% block body %} 
    {% include "navbar.html.twig" %}
    
    <div class="container d-flex justify-content-center align-items-center" style="height: 100vh; background-color: #f8f9fa;">
        <div style="background-color: #fff; padding: 20px; border-radius: 10px; box-shadow: 0 0 10px rgba(0, 0, 0, 0.1); width: 100%;">
            <h2 style="margin-bottom: 20px; text-align: center;">Liste des Clients</h2>
            <form action="" method="POST">
                <div class="row mb-3">
                    <label for="newRole" class="col-sm-2 col-form-label">Role</label>
                    <div class="col-sm-10">
                        <select name="newRole" id="newRole" class="form-select">
                            <!-- Select dropdown for role modification -->
                            <option value="Intervenant">Client</option>
                            <option value="Intervenant">Intervenant</option>
                            <option value="Standardiste">Standardiste</option>
                            <option value="Admin">Admin</option>
                        </select>
                    </div>
                </div>
                <div class="row">
                    <div class="col-sm-10 offset-sm-2"> <!-- Offset for the button to align with the select -->
                        <button type="submit" name="update" class="btn btn-primary">Modifier</button>
                    </div>
                </div>
            </form>
            
            <div class="text-center mt-3"> <!-- Adding margin-top -->
                <a href="viewuser.php" class="btn btn-info">Liste des utilisateurs</a>
            </div>
        </div>
    </div>

    <footer>
        {% include "footer.html.twig" %}
    </footer>
{% endblock %}
